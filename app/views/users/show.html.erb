
<div class="my-page">
  <div class="my-name">
    <%= "#{current_user.nickname}  サ活一覧"%>
  </div>
  <div class="my-sauna">
    <% @saunas.each do |sauna| %>
      <ul>
        <li class="my-sauna-shop"><%=link_to sauna.shop , sauna_path(sauna.id)%></li>
      </ul>
    <%end%>
  </div>
</div>